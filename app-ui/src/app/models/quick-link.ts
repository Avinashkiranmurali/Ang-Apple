export interface QuickLink {
  locale: string;
  varId: string;
  programId: string;
  linkCode: string;
  linkText: string;
  linkUrl: string;
  order: number;
  showUnauthenticated: boolean;
  display: boolean;
}
